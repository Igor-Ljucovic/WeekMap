services:
  backend:
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: "Server=db;Database=WeekMap_Dev;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True;"
